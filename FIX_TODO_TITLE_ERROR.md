# إصلاح خطأ "Value missing for ToDo: Title"

## ✅ تم إصلاح المشكلة!

تم إصلاح الخطأ الذي كان يظهر عند محاولة تعيين الرسالة.

---

## 🔄 خطوات تطبيق الإصلاح:

### 1. إعادة تحميل الصفحة بقوة (Hard Reload)

**في المتصفح، اضغط أحد المفاتيح التالية:**

- **Windows/Linux**: `Ctrl + Shift + R` أو `Ctrl + F5`
- **Mac**: `Cmd + Shift + R`

أو:

1. افتح أدوات المطور (Developer Tools): `F12`
2. انقر بزر الماوس الأيمن على زر التحديث 🔄
3. اختر **"Empty Cache and Hard Reload"** (إفراغ الذاكرة المؤقتة وإعادة التحميل)

---

## 🧪 اختبار الإصلاح:

### الخطوات:

1. **افتح الرسالة التجريبية**:
   ```
   http://localhost:8000/app/incoming-letter/INC-LTR-2025-00027
   ```

2. **انقر على "الإجراءات" → "عرض وتعيين"**

3. **املأ البيانات**:
   - اختر مستخدم من قائمة "معين إلى"
   - (اختياري) حدد تاريخ استحقاق
   - (اختياري) أضف ملاحظة

4. **انقر على زر "تعيين"**

### ✅ النتيجة المتوقعة:

- رسالة نجاح: **"تم تعيين الرسالة بنجاح"** ✅
- إغلاق النافذة تلقائياً
- إنشاء مهمة (ToDo) للمستخدم المعين
- تحديث حقل "معين إلى" في الرسالة

### ❌ إذا ظهر الخطأ مرة أخرى:

تأكد من:
1. إعادة تحميل الصفحة بقوة (Hard Reload)
2. إفراغ ذاكرة التخزين المؤقت للمتصفح
3. إعادة تشغيل خادم Frappe إذا لزم الأمر:
   ```bash
   bench restart
   ```

---

## 🔍 التفاصيل التقنية:

### المشكلة:
كان API `frappe.desk.form.assign_to.add` يتطلب حقل `title` إلزامي لإنشاء ToDo، لكن الكود لم يكن يرسله.

### الحل:
تم إضافة الحقل `title` في استدعاء API:
```javascript
title: frm.doc.subject || __('Incoming Letter Assignment')
```

الآن يستخدم النظام موضوع الرسالة كعنوان للمهمة، أو نص افتراضي إذا لم يكن هناك موضوع.

---

## 📞 الدعم:

إذا استمرت المشكلة بعد تطبيق الخطوات أعلاه، يرجى:
1. التحقق من سجلات المتصفح (Console) للأخطاء
2. التحقق من سجلات Frappe
3. التواصل مع فريق الدعم

---

**تاريخ الإصلاح**: 2025-12-09
**الإصدار**: 1.0.1
